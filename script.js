const menuButton=document.querySelector('.menu-btn');const navLinks=document.querySelector('.nav-links');if(menuButton&&navLinks){menuButton.addEventListener('click',()=>navLinks.classList.toggle('open'));navLinks.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>navLinks.classList.remove('open')))}const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();const OLD_PHONE='60129153527';const NEW_PHONE='60128681339';const OLD_DISPLAY='+60 12-915 3527';const NEW_DISPLAY='+60 12-868 1339';document.querySelectorAll('a[href]').forEach(a=>{const href=a.getAttribute('href')||'';if(href.includes(OLD_PHONE))a.setAttribute('href',href.replaceAll(OLD_PHONE,NEW_PHONE))});const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.forEach(n=>{if(n.nodeValue&&n.nodeValue.includes(OLD_DISPLAY))n.nodeValue=n.nodeValue.replaceAll(OLD_DISPLAY,NEW_DISPLAY)});document.querySelectorAll('script[type="application/ld+json"]').forEach(s=>{if(s.textContent&&s.textContent.includes(OLD_PHONE))s.textContent=s.textContent.replaceAll(OLD_PHONE,NEW_PHONE)});